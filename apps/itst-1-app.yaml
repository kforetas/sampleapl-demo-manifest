apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: deploy-sampleapl-demo-itst-1
  namespace: openshift-gitops
spec:
  project: default
  source:
    repoURL: 'https://github.com/kforetas/sampleapl-demo-manifest'
    targetRevision: HEAD
    path: manifest/overlays/itst-1
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: demo-itst-1
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
